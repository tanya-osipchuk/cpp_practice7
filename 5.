#include <iostream>
#include <fstream>
using namespace std;

int main()
{
    setlocale(LC_ALL, "rus");
    ofstream fout("c:\\1\\cppclion.txt");
    string s;
    cout <<"Input message: ";
    cin >> s;
    fout << s;
    fout.close();
    system("pause");
    return 0;
}
